# requirements.txt — VoxDesk Sprint 5.3 Runtime
#
# NOTE:
# PyTorch is intentionally NOT installed from this file.
# Install PyTorch separately with the official CUDA 12.8 wheel index for RTX 5080:
#
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#
# Reason:
#   torch>=2.5.0 can install CPU-only or incompatible wheels depending on pip/index state.
#   RTX 5080 (Blackwell, SM 12.0) requires CUDA 12.8-compatible PyTorch build.

# Backend / API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
websockets>=14.0

# Local sidecar HTTP client (localhost-only — Sprint 8)
# Used by LocalLlamaServerProvider and SidecarManager for http://127.0.0.1 communication ONLY.
# NOT used for external/cloud API calls.
httpx>=0.27.0

# Local LLM provider — llama-cpp-python (GGUF inference)
# Sprint 4: Pure local provider, no external API dependency.
# CUDA build required for GPU acceleration:
#   $env:CMAKE_ARGS = "-DGGML_CUDA=on -DCMAKE_CUDA_ARCHITECTURES=120"
#   pip install llama-cpp-python --no-cache-dir --force-reinstall
llama-cpp-python

# Screen capture / vision utilities
dxcam>=0.3.0
Pillow>=11.0.0
numpy>=2.0,<3

# STT
faster-whisper>=1.1.0

# TTS / audio IO
kokoro>=0.9.0
soundfile>=0.13.0
sounddevice>=0.5.0
av>=14.0

# Translator (MarianMT TR→EN)
transformers>=4.40.0
sentencepiece>=0.2.0
sacremoses>=2.1.0

# Config / hotkeys / tray
pyyaml>=6.0
keyboard>=0.13.5
pystray>=0.19.0

# Tests
pytest>=8.0.0
pytest-asyncio>=0.25.0
pytest-benchmark>=4.0.0
pytest-cov>=6.0.0
pytest-timeout>=2.3.1
